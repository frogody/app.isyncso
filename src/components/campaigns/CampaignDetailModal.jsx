import React from "react";

// This component intentionally renders nothing to remove the popup overlay UI.
// Campaign work now happens only in the inline detail panel on the page.
export default function CampaignDetailModal() {
  return null;
}